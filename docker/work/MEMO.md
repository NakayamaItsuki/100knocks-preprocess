
# Dockerの起動
```bash
docker compose up -d
```

# Jupyterサーバの環境を利用
1. 既存のJupyterサーバを選択し，URLに下記を入力
    ```
    http://localhost:8888/
    ```
2. /opt/conda/bin/python (localhost)を選択

# CursurのCopilotを停止
左下のCursur TabからDisableを選択